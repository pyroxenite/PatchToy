/**
 * Configuration
 * Set API_URL to your backend API Gateway URL when backend is deployed
 * Leave as null to disable cloud features
 */
export const config = {
  // Set to null to disable (button will be grayed out)
  API_URL: "https://7c11mva4zg.execute-api.eu-west-3.amazonaws.com/prod",
};
